# Backlog de producto — pendientes e hitos

**Propósito:** lista viva de lo que falta por producto (no es definition-of-done por ticket). Actualizar al cerrar hitos o al priorizar sprints.

**Agentes Cursor (varios a la vez):** leer **`TASKS/leer-primero.md`** primero — reparto típico: cierre **1.0.0** (`plan-release-1.0.0.md`) vs siguiente ítem **§5** del backlog.

**Release 1.0.0:** alcance «probable de punta a punta», smoke manual y qué queda **fuera** del tag → **`TASKS/plan-release-1.0.0.md`**. Flujo: cerrar núcleo §1 del plan + smoke §2 + `npm run test:ci` → tag **v1.0.0** → seguir **§5** y §4.3 **D** / operación según prioridad (mucho de §4.3 **B/C** ya está en código; ver tabla §4.3 en este documento).

**Prioridad acordada:** la **definición y cableado de nuevos eventos** del motor transaccional (ampliar matriz §1.2, nuevos disparadores, hooks y semillas) queda **al final** del roadmap en curso; no bloquea el resto de hitos (OTA, i18n, dominio, tarifas, etc.).

**Contexto reciente ya entregado (sesión 2026):** normas del alojamiento en SPA (`/normas-alojamiento`), API `houseRules`, reglas en ficha SSR con modal «Más información» bajo «Cosas que debes saber», misma UX en página SSR `/reservar`, JSON-LD alineado con `petsAllowed` / `smokingAllowed` desde normas mergeadas, fuentes SSR sin Google Fonts por CSP, panel admin (`frontend/index.html`) sin cargar Inter desde Google (evita choque CSP), plantillas de texto en normas.

**Hecho (2026-04-24, IA venta / listado):** en tarjetas de `busquedaGeneral` (`suitemanagerApiController`) e inventario PG (`publicAiInventoryPg`): JOIN `e.configuracion AS empresa_configuracion` + `enrichPropertyRowsForPublicAi` / `buildListingCardForAi` aplican `enrichUbicacionFromEmpresaConfig` tras metadata de la propiedad (misma lógica que detalle).

**Hecho (2026-04-24, IA venta / payload):** `rating` alineado a `resenas.promedio_general` con `rating_fuente`; `senales_ranking_ia` (heurístico); `precio.por_estadia` en detalle cuando hay cotización OK; entorno turístico ampliado (geo + tags ski/termas/valle); batch reseñas en listado (`obtenerPromedioResenasBatchPorPropiedades`).

**Hecho (2026-04-24, IA venta / disponibilidad y reserva):** `GET /api/disponibilidad` → `publicAiDisponibilidadService` (`disponibilidad_ia_v1`): restricciones con códigos (`evaluarRestriccionesReservaWebCodigo`), fotos preview PG, `precio_total_estadia_clp` por alojamiento libre, motivos `sin_tarifa_en_fechas` / `ocupado_o_bloqueado`, `empresa.is_demo_heuristica`, endpoints galería/detalle; `/ai/busqueda-general` query `vibe`; detalle `requiere_confirmacion_final`; reserva pública valida email y teléfono básico.

**Hecho (2026-04-24, IA venta / cotización previa reserva):** `POST /api/reservas/cotizar` y `POST /api/public/reservas/cotizar` → `publicAiReservaCotizacionService.cotizarReservaIaPublica` (dry-run: mismas validaciones/calendario/precio que `createPublicReservation`, desglose checkout + política cancelación; sin cupones; no persiste). Cableado en `suitemanagerApiController`, `routes/api.js`, `publicRoutes.js`; OpenAPI ChatGPT/Gemini **1.4.4** (cotizar paths 1.4.2, `booking_workflow` en detalle 1.4.3, `endpoints` + `requiere_confirmacion_reserva` en disponibilidad 1.4.4); respuesta 201 incluye `sugerencia_previa`. Detalle legacy Firestore: `booking_workflow.paso_2b`. Disponibilidad: `endpoints.cotizar_reserva_dry_run` en `publicAiDisponibilidadService`.

**Pausa 2026-04-22 (§4 legal / checkout):** panel «Legal y checkout público» en web pública unificada, guardado de `legal` vía `PUT /api/website/home-settings`, validación estricta de `lineasExtra` (front + back, ver subsección §4.1). Retomar desde la lista **«Pendiente al retomar»** allí.

**Aviso 2026-04-24 — identificadores vs nombres (todos los agentes):** regla vigente: **ids / `semantica` / disparadores en negocio; nombres solo UI.** **Fase 1 entregada (código):** `estado_gestion_id` en PostgreSQL + escrituras/lecturas y SPA parcial (gestionar reservas, gestión diaria avanzar estado, calendario). **Deploy:** aplicar migración `node backend/scripts/apply-sql-migration.js db/migrations/reservas-estado-gestion-id.sql` en cada entorno con Postgres. Detalle y pendientes: **`TASKS/audit-identificadores-vs-nombres-ui.md`** (§ avance). Coordinación: **`TASKS/coordinacion-cursor-claude-ia-venta.md`** §2.

**Hecho (2026-04-25, identidad empresa PG-first):** autenticación de panel (`authMiddleware`) resuelve `empresaId`/`nombreEmpresa` **solo desde PostgreSQL** (`usuarios` + `empresas`), sin fallback Firestore; `empresaService` normaliza `subdominio` al guardar y valida unicidad de `nombre` + `subdominio`; `authService.register` valida nombre único. Migración aplicada: **`db/migrations/empresas-identidad-unica.sql`** (índices únicos case-insensitive para `nombre` y subdominio efectivo, incluyendo legacy en `configuracion.websiteSettings.*` + normalización de duplicados existentes).

**Hecho (2026-04-25, operación de pagos manuales v1.1):** se explicita operación **sin pasarela integrada** (solo registro de pagos recibidos): medios permitidos `Efectivo`, `Transferencia`, `Tarjeta Débito (POS externo)`, `Tarjeta Crédito (POS externo)`; validación server-side en `transaccionesService`; endpoint catálogo `GET /gestion/medios-pago-manuales`; validación suave en UI para sugerir comprobante según medio (sin bloqueo); transacciones admiten `observacion` corta (`metadata.observacion`) visible en Gestión Diaria y modales de reservas. Se preserva lógica de grupo existente (`id_reserva_canal`) y prorrateo proporcional de abonos en detalle.

**Hecho (2026-04-25, Google Hotels checklist + SSR i18n + bandeja + hardening):** script remoto `backend/scripts/verify-google-hotels-feed-checklist.js` (env `GH_FEED_BASE_URL` / `GH_FEED_TOKEN`) y **§9** en `TASKS/checklist-onboarding-google-hotel-center.md`; `res.locals.htmlLang` central en `website.context.js`; copy EN/ES ampliado en `home.ejs`, `contacto.ejs`, `404.ejs`, `confirmacion.ejs`, `partials/header.ejs`, `partials/footer.ejs`, `partials/property-card.ejs` (desde home), `propiedad.ejs` (`lang` + `og:locale`); bandeja `/comunicaciones`: aviso de error con reintento de carga, vacíos con icono, toasts en reintentos (fila y lote) en `comunicaciones.js`; comentarios multi-tenant en `website.property.js` (calendario), `website.seo.js` (feed contenido), JSDoc en `obtenerOcupacionCalendarioPropiedad` (`publicWebsiteService.js`).

---

## 1. Mensajería, correo transaccional y alertas

**Objetivo:** un módulo coherente de comunicaciones (huésped + operación interna), no solo piezas sueltas.

| Hito | Descripción | Base en código (punto de partida) |
|------|-------------|-----------------------------------|
| **1.1 Bandeja / historial de mensajes** | **Hecho (v1):** bandeja por tenant en SPA `/comunicaciones` (lista, filtros **estado/evento/tipo**, paginación) + `GET /api/comunicaciones`; enlace a cliente con `?tab=correos`; relación **reserva** con enlace a `gestionar-reservas` si `relacion_id` es UUID. **Estados:** `enviado` \| `fallido` \| `pendiente` \| `recibido` \| `leido`. **Reintento (v1.1):** si falla el envío del motor transaccional, se registra fila `fallido`; `POST /comunicaciones/:id/reintentar` + botón **Reintentar** en bandeja (email + fallido + relación `reserva`; eventos mapeados a disparador o reenvío por `plantilla_id`). **Hecho (v1.1e):** UX bandeja cerrada: enlace **propuesta** a gestión; estados vacíos con guía; pestaña **Por conversación** sincroniza `?vista=hilos`; debounce en filtro evento (lista); texto de ayuda en hilos. **Hecho (v1.1b):** `POST /comunicaciones/reintentar` (lote máx. 25). **Hecho (v1.1c):** hilos + vista por conversación. **Hecho (v1.1d):** reintento ampliado en `comunicacionesRetryService`: propuesta (`relacion_tipo` propuesta + filas `Propuesta`); correos fallidos sin relación de reserva vía disparador (p. ej. `consulta-web-publica`) o `plantilla_id` + variables mínimas cliente/empresa. **Hecho (v1.1f 2026-04-25):** si falla la carga de lista o hilos: banner superior con mensaje + **Reintentar carga**; vacíos con icono y título; toasts no bloqueantes para reintento en fila (estado «Reintentando…») y resumen de lote (sustituye `alert` en esos flujos). | `comunicacionesService.js`, `comunicacionesRetryService.js`, `routes/comunicaciones.js`, `comunicaciones.js`, `transactionalEmailService.js`, `perfilCliente.js`. |
| **1.2 Correos transaccionales completos** | **Hecho (v1 parcial):** matriz documentada en código (`transactionalEmailEventMatrix.js`) + `GET /plantillas/matriz-eventos-correo` (cobertura por tenant: plantilla PG activa por disparador). UI en **Configuración empresa** (`empresa.js`): tabla «Matriz de correos transaccionales». Flujos ya cableados: confirmación/cancelación/modificación reserva, recordatorio pre‑llegada, post‑estancia/evaluación, consulta web (huésped + interna), propuesta, digest operación, copia admin reserva confirmada → `notificacion_interna` (fallback HTML si no hay plantilla). **Hecho (v1.2b):** reserva pública IA (`createPublicReservation` en `publicAiController`) usa **`enviarEmailReservaConfirmada`** → `enviarPorDisparador` + plantillas **PostgreSQL** (misma vía que gestión); extras IA (datos bancarios, plazo 48h, seña 10%) vía argumento `extras` en `gestionPropuestas.email.js`; ya no se busca plantilla en Firestore ni HTML fallback propio del controlador. Respeta `correosAutomaticosActivos` / categorías del motor. **Diferido al final (acuerdo 2026-04-22):** nuevos eventos / disparadores / filas de matriz / hooks — ver nota al inicio de este documento. | `publicAiController.js`, `gestionPropuestas.email.js`, `transactionalEmailService.js`, `transactionalEmailEventMatrix.js`, `routes/plantillas.js`, `empresa.js`. |
| **1.3 Plantillas editables por evento** | **Hecho (v1):** catálogo `DISPARADOR_CATALOGO` / `DISPARADOR_KEYS`; APIs `disparadores-motor` y `por-disparador/:key`; modal plantillas (email) desde API. **`prepararMensaje`** fusiona plantillas por **tipo Firestore** + plantillas PG cuyo `email_config` tiene disparador: default **salida** → `post_estadia_evaluacion`; cuerpo opcional `motorDisparador` (`false` = solo Firestore, string = disparador explícito). **`GET /plantillas`** acepta `?motorDisparador=` (coma o `+` para varios) y fusiona Firestore + PG por disparador (`obtenerPlantillasPorEmpresaFusionandoMotor`). **Gestión diaria** (`mensajeModal.js`): selector de motor + preview con etiquetas motor (`[NOMBRE_CLIENTE]`, `[LINK_RESEÑA]`, etc.). **Agregar propuesta** (`propuesta.*`): selector motor + lista de plantillas vía query. **Enviar mensaje al huésped** (`enviarMensajeCliente.js`): plantillas reales + mismo selector. **Config. envío correo plantillas** (`plantillas.emailConfig.modal.js`): catálogo disparadores vía `shared/plantillasMotor.js`. `prepararPlantillasPorDisparadorMotor` sigue disponible. | `mensajeService.js`, `routes/mensajes.js`, `mensajeModal.js`, `plantillasService.js`, `routes/plantillas.js`, `frontend/src/shared/plantillasMotor.js`. |
| **1.4 Alertas gestión diaria** | **Hecho (v1 parcial):** digest diario (`runDigestOperacion`): llegadas/salidas confirmadas hoy, propuestas abiertas. **Hecho (v1.4b):** query agregada por empresa (check-out distintos, `metadata.estadoPago` pendiente, bloqueos hoy, correos fallidos 24 h; lote con fallos recientes). **Hecho (v1.4c):** correo digest vía **`enviarPorDisparador`** con disparador **`digest_operacion_diario`** (variables `[DIGEST_*]` + resumen); fallback texto/HTML anterior si no hay plantilla PG; excepción en motor para no depender del interruptor global huésped. **Web push (opcional):** `webPushDigestService` + VAPID env; rutas `GET /empresa/web-push-config`, subscribe/unsubscribe/test; SW `/sw-webpush.js`; UI **Configuración empresa** (`digestOperacionDiario`, `digestWebPushActivo`, suscribir/prueba). **Hecho (2026-04-24):** métricas iCal en PG vía **`historial_cargas`** al cerrar `sincronizarCalendarios` (`total_procesadas` = URLs intentadas, `total_creadas`, `errores` JSON); digest suma nuevas 7d y cuenta sync con error 7d; etiquetas `[DIGEST_ICAL_NUEVAS_RESERVAS_7D]`, `[DIGEST_ICAL_SINCRONIZACIONES_CON_ERROR_7D]`. **Hecho (2026-04-25):** alertas operativas adicionales en digest: `digestPagoPendienteVencido` (seña/pago pendiente con `vencimientoPago` ya expirado) y `digestLlegadasMananaSinHoraEstimada` (llegadas de mañana sin `reservaWebCheckout.horaLlegadaEstimada`); push incluye ambas cuando aplican; etiquetas `[DIGEST_PAGO_PENDIENTE_VENCIDO]` y `[DIGEST_LLEGADAS_MANANA_SIN_HORA_ESTIMADA]`; tests `test-digest-etiquetas-alertas.js` + smoke SQL `smoke-digest-ical-metrics.js` actualizado. | `icalService.js`, `scheduledTransactionalEmails.js`, `plantillasEtiquetasCatalog.js`, `plantillasService.js`, `transactionalEmailService.js`, `webPushDigestService.js`, `routes/empresa.js`, `empresa.js`. |
| **1.5 Consultas desde web pública** | **Hecho (v1):** `POST` contacto SSR, lead en `clientes`, disparadores `consulta_contacto` / interna y registro en `comunicaciones` vía `transactionalEmailService` + `publicContactoService.js`. | `backend/services/publicContactoService.js`, `backend/routes/website.home.js`, `backend/views/contacto.ejs`. |
| **1.6 Preferencias por empresa** | **Hecho (v1):** `websiteSettings.email` en `configuracion` (merge en `empresaService`): `correosAutomaticosActivos`, `replyToOverride`, `nombreRemitenteOverride`. **Hecho (v1.6b):** `idiomaPorDefecto` (`es` \| `en`) persistido; `correosAutomaticosCategorias` (`reservasTransaccionales`, `recordatorioPreLlegada`, `evaluacionPostEstadia`) aplicado en `transactionalEmailService.enviarPorDisparador`. UI en **Configuración empresa** (`empresa.js`). **Hecho (v1.6c):** SSR sitio público: `res.locals.htmlLang` desde `idiomaPorDefecto` (`website.context.js`, también en fallback SSR optimizado) + `lang` en EJS (`home`, `propiedad`, `contacto`, `reservar`, `confirmacion`, `404`, **cabecera/pie** compartidos). **Hecho (v1.6c ext. 2026-04-25):** más copy visible EN/ES en `home.ejs` (hero búsqueda, strip, resultados vacíos, grupo, about), `contacto.ejs` (meta, hero, bloques), `confirmacion.ejs` (título, depósito, detalle, totales con locale), `404.ejs`, `partials/property-card.ejs` vía `htmlLocale` desde home, `og:locale` alineado en `propiedad.ejs`. **Correos:** `construirVariablesDesdeReserva` expone `idiomaPorDefecto`, `localeFecha`, `htmlLang`; fechas y montos CLP (`precioFinal`, etc.) con `Intl` según locale; `fechasEstadiaTexto` en inglés usa «to». **Reintento propuesta** (`comunicacionesRetryService._variablesParaPropuestaRetry`): mismo criterio de idioma/locale para fechas, montos y `fechaVencimiento`. **Calendario ocupación:** `GET /propiedad/:id/calendario-ocupacion` acepta query opcionales `fechaLlegada` y `fechaSalida` (junto a `from`/`to`, checkout exclusivo) para nightly con promo de tarifa; `public-booking-calendar.js` las envía al tener rango completo y refresca tras elegir o limpiar. **Hecho (v1.6d parcial 2026-04-24):** fallback de **asunto** si plantilla vacía (`transactionalEmailFallbackSubjects.js`); confirmación reserva **legado** (`gestionPropuestas.email.js`): asunto + cuerpo texto según `idiomaPorDefecto` (`en` \| `es`). **Hecho (v1.6e 2026-04-25):** copy de reintento para `consulta-web-publica` sensible a idioma (`es/en`) en `comunicacionesRetryService`, y `resumenValores` de variables motor en inglés (`Total amount`) cuando `idiomaPorDefecto='en'` (`transactionalEmailService`). | `website.context.js`, `backend/views/*.ejs`, `partials/header.ejs`, `partials/footer.ejs`, `partials/property-card.ejs`, `transactionalEmailService.js`, `website.property.js`, `website.shared.js`, `public-booking-calendar.js`, `empresa.js`, `comunicacionesRetryService.js`. |
| **1.7 Evaluación y reseña post‑estancia (reenvío hasta completar)** | **Hecho (v1):** job `runPostEstadiaEvaluacion` con `emailAutomations.evaluacionPostEstadia` (`diasTrasCheckout`, `maxIntentos`, `ventanaDias`, `urlResenaExterna`); merge en `empresaService`; UI en **Configuración empresa** (`empresa.js`). Comunicaciones: `evaluacion-pendiente`, `evaluacion-formulario-abierto`, `evaluacion-completada`. **Hecho (v1.7b):** `plantillasPorIntento` (UUID PG por orden de intento) + `plantillaIdOverride` en motor; **pixel** `GET /r/:token/open.gif` → evento `evaluacion-correo-abierto` (`pixelAperturaActivo` en config, por defecto activo). | `scheduledTransactionalEmails.js`, `transactionalEmailService.js`, `resenasService.js`, `backend/index.js`, `empresa.js`, `clientes.modals.js`. |

**Criterio de “módulo listo”:** todo evento crítico de reserva y operación tiene plantilla, envío, registro en `comunicaciones` y visibilidad en UI; **incluye** el flujo de **evaluación/reseña** con reintentos controlados hasta cierre o completado.

---

## 2. Tarifas: ofertas destacadas (SSR + marketplace)

| Hito | Descripción |
|------|-------------|
| **2.1 Modelo de oferta** | **Hecho (v1):** `tarifas.metadata.promo` (%, `minNoches`, `etiqueta`, `activa`; ventanas JSON estancia/reserva en SSR). **Hecho (v2.1b):** matriz de tarifas: modo **%** / **monto fijo por noche** (`montoFijoDescuento`, `tipoDescuento`), fechas de ventana de **estancia** editables por fila; `promocionesDisplayService` y SQL marketplace consideran monto fijo. **Hecho (v2.1c):** en matriz, inputs opcionales **ventana de reserva** (`fechaReservaDesde` / `fechaReservaHasta`, alineados con `evaluarPromoTarifa`). | `matriz.js`, `promocionesDisplayService.js`, `marketplaceService.js`, `tarifasService.js`. |
| **2.2 Señalización en SSR** | **Hecho (v1):** `promocionesDisplayService` aplica promo de tarifa (prioridad) + `websiteSettings.marketing.promocionesDestacadas` en búsqueda home SSR y ficha `propiedad`. **Widget reserva:** nota aclaratoria bajo el total (total/abono = tarifa vigente; promo publicada es referencia). |
| **2.3 Marketplace** | **Hecho (v1):** listado + API con precio «desde» y `sort=valor` usando promo en `tarifas.metadata` (SQL) + badge. **Hecho (v2.3b):** precio «desde» y badge % consideran también **promo monto fijo** (equivalente % para badge). **Hecho (v2.3c):** misma lógica de promo de tarifa que el display en **checkout web** (`publicWebsiteService.calculatePrice`), motor reservas/IA (`calculoValoresService.calculatePrice`) y nightly del calendario cuando se envían `fechaLlegada`/`fechaSalida` (ver 1.6c). **Hecho (v2.3d):** sin fechas en query, **home SSR** usa próximo fin de semana como contexto de estancia solo para `aplicarPromocionesDisplayCompleto` (precios «desde» alineados a promo de tarifa). **Más alojamientos** en ficha reciben `fechaEstanciaLlegada` / `fechaEstanciaSalida` desde la misma resolución que la ficha (`website.property.page.js` → `obtenerMasAlojamientosParaFichaSSR`). **Hecho (v2.3e 2026-04-24):** `GET /widget-reserva-ayuda.json` + **`widget-reserva-embed.js`**: CTA y fechas por atributos (`data-fecha-llegada` / `data-fecha-salida`); **`data-mostrar-tarifa="1"`** + `GET /propiedad/{id}/widget-tarifa.json` muestran total estimado bajo el enlace (sin iframe); ayuda JSON y panel unificado documentan atributos. | `website.home.helpers.js`, `website.property.page.js`, `publicWebsiteService.js`, `marketplaceService.js`, `promocionesDisplayService.js`, `website.seo.js`, `website.property.js`. |
| **2.4 Reglas de conflicto** | **Hecho (v1):** promo ligada a tarifa gana sobre promo de marketing en display SSR; sin tocar `valorHuesped` ni precios por canal persistidos. |

---

## 3. Dominio propio por cliente (principal) + instructivo DNS

| Hito | Descripción |
|------|-------------|
| **3.1 Documentación en producto** | **Hecho (v1):** en configuración web unificada (`webPublica.general.unified.*`), panel **Guía DNS** con pasos (Nic Chile, Cloudflare, apex vs www, CNAME/ALIAS). **`GET /api/website/dns-render-hint`** devuelve el hostname CNAME público (`getRenderCnameHostname` en `renderDomainService.js`); el front lo muestra en vivo. Tras guardar dominio, el bloque amarillo existente muestra tipo/host/valor de `syncDomain`; si Render falla, mensaje en **`dns-sync-error`**. **Hecho (v3.1b):** botones **Copiar** (hostname Render, valor CNAME, fila TSV tipo/host/valor). **Hecho (2026-04-24):** en error **`PUT /website/home-settings`** (400/500), el panel **`dns-sync-error`** muestra mensaje API + `details`. **Hecho (2026-04-24 — §3.1 legacy):** eliminados **`webPublica.general.wizard.js`** y **`webPublica.general.view.js`** (sin imports en el SPA; entrada real `websiteGeneral.js` → `webPublica.general.js` → unificado); **`node backend/scripts/verificar_wizard_actual.js`** valida módulos unificados + ausencia de legacy; al pulsar **Guardar** se limpia **`dns-sync-error`** antes del `PUT` para no mezclar errores viejos con un reintento. |
| **3.2 Automatización ya existente** | Registrar/verificar dominio en Render al guardar `websiteSettings` (flujo actual). | `renderDomainService.js`, `syncDomain` en `config.routes` / `home-settings`. |
| **3.3 Dominio “principal”** | **Hecho (v1):** host canónico desde `websiteSettings.general.domain` / `websiteSettings.domain` / `empresas.dominio` (no aplica a hosts `*.suitemanagers.com` / `*.onrender.com`). **301** al canónico en `tenantResolver.js` si el `Host` difiere (solo GET/HEAD; no aplica con `?force_host=` ni cookie `force_host`). Resolución tenant ampliada: **www ↔ apex** en `obtenerEmpresaPorDominio` (`empresaService.js` + `websiteHostCanonical.js`). **Estado Render:** `GET /api/website/domain-status` (verificación / SSL). |
| **3.4 SSL y fallos** | **Hecho (v1):** panel «Verificación en Render (DNS / SSL)» en configuración web unificada; mensajes según `verificationStatus` / `uiState`; **`POST /api/website/retry-domain-sync`** reintenta `syncDomain` sin cambiar la BD. **Parcial:** errores de API al guardar dominio siguen en `dns-sync-error` (§3.1). |

---

## 4. Recomendaciones estilo OTA (Booking, Airbnb, Expedia, Agoda, Google Hoteles)

Módulos que suelen ser **esperados** en un PMS + web directa y que conviene valorar para el backlog:

| Módulo | Por qué importa |
|--------|------------------|
| **Políticas de cancelación** (tipos + plazos + texto legal) | Comparables en todas las OTA. **Hecho (v4 parcial SSR):** HTML/texto en ficha, **/reservar**, **/confirmacion** (`politicaCancelacionHtml` / `Etiqueta`). **Hecho (v4 parcial resumen):** aviso corto vía `buildAvisoPoliticaCancelacion` + partial `aviso-politica-cancelacion.ejs` si `legal.politicaCancelacionModo` es `gratis_ilimitada` o `gratis_hasta_horas` (con `politicaCancelacionHorasGratis`). **Hecho (v4 panel):** en **Configurar sitio web público** (unificado), bloque «Legal y checkout público» + `PUT /website/home-settings` con `legal` (merge con `websiteSettings.legal` existente). **Sugerencia producto (por defecto):** `politicaCancelacionModo: 'gratis_hasta_horas'`, `politicaCancelacionHorasGratis: 48` (opt-in al guardar; no se fuerza en migración). **Hecho (v4 por tarifa, 2026-04-24):** `tarifas.metadata.politicaCancelacion` (`modo`: `inherit` \| `texto_solo` \| `gratis_hasta_horas` + `horasGratis` \| `gratis_ilimitada`); si **todas** las noches de la estadía usan la **misma** tarifa con override ≠ `inherit`, el aviso SSR usa ese modo/plazo; matriz de tarifas (`matriz.js`) edita el bloque; `reservas.metadata.politicaCancelacionCheckout` guarda snapshot al crear reserva web (`crearReservaPublica`). **Hecho (v4 presupuesto, 2026-04-24):** `generarTextoPresupuesto` + placeholder `[DESGLOSE_PRECIO_PRESUPUESTO]` (`mensajeService.js`); JSON de presupuesto opcional `desglosePrecioCheckout` (`presupuestosService.js`). **Hecho (§4.3 B, 2026-04-24):** texto largo multi-tarifa (mayoría de noches + bloques secundarios SSR) y **`metadata.nombre`** editable en matriz («Nombre en web público») para titular tramos. |
| **Impuestos y tasas** (IVA, tasa municipal, fee limpieza) | **Hecho (v4 parcial):** `checkoutDesgloseService.js` — `modelo` `cl_iva_incluido` (total con IVA, tasa 0–50%) o `sin_desglose` (solo líneas extra). **`lineasExtra`** configurables: `porcentaje_total`, `porcentaje_neto` (sobre neto IVA o sobre total si no hay IVA), `monto_fijo`, `por_noche` (`montoPorNocheCLP`), **`por_persona_noche`** (`montoPorPersonaNocheCLP` × huéspedes × noches; SSR `/reservar`, `/confirmacion`, `crearReservaPublica`, widget `booking.js`); `notaPie` / `notaDebajo`. SSR **/reservar**, **/confirmacion** (noches y huéspedes desde query / reserva), widget ficha; `crearReservaPublica` sigue guardando neto/IVA cuando aplica IVA. **Hecho (v4 panel):** bloque unificado + **`lineasExtraValidation.js`** (front `shared/` y back `services/`, misma regla): validación por fila al guardar y **`PUT /website/home-settings`** 400 con `details` si falla; respuesta persistida con filas ya sanitizadas. **Hecho (v4 presupuesto, 2026-04-24):** texto generado y JSON presupuesto usan el mismo `buildDesglosePrecioCheckout` con noches y huéspedes. **Hecho (v4 coherencia, 2026-04-24):** `validateDesglosePrecioCheckoutCoherencia` — suma de `% sobre total` y `% sobre neto` ≤100%; escenario de referencia 1M CLP / 7 noches / 4 huéspedes: suma de montos de líneas extra no supera el total; comprobación neto+IVA; panel (`collectLegalFromForm`) + **`PUT /api/website/home-settings`**; test `test-desglose-coherencia.js`. **Hecho (2026-04-24 — reconciliación reserva web):** `verificarReconciliacionPrecioReservaPublica` vs `calculatePrice` (suma multi-ID); **409** si `precioFinal` desalineado; `metadata.precioCheckoutVerificado` + `reservaWebGrupo`; cupón **`porcentaje`** o **`monto_fijo`** (`tipo_descuento`, monto en `descuento`); preview **`POST /preview-precio-reserva-checkout`**; transacción insert + marcar cupón. |
| **Depósito de garantía / preautorización** | **Descartado (2026-04-25):** no se usará pasarela/preauth integrada en esta fase. Operación manual con medios permitidos y registro en transacciones (`GET /gestion/medios-pago-manuales`). |
| **Política de menores y camas extra** | **Hecho (v4 parcial 2026-04-24):** formulario **/reservar** + `metadata.reservaWebCheckout` + recargos opcionales `recargoMenorNocheCLP` / `recargoCamaExtraNocheCLP` (CLP × unidad × noche) en panel unificado; reconciliación y **`POST /preview-precio-reserva-checkout`** incluyen recargos **antes** del cupón; `checkout.js` refresca total si hay recargos configurados. **Pendiente:** precio distinto por edad de menor o cupos por inventario físico de camas. |
| **Feed de disponibilidad y precios** (ARI) | **Hecho (v4 parcial 2026-04-25):** `GET /feed-ari.xml` en rutas SSR tenant (`website.seo.js`) con `mode` (`website` \| `google_hotels`), `days` 14–365 (default 180, clamp), token opcional por empresa (`websiteSettings.integrations.ariFeedToken`) y ayuda pública `GET /widget-reserva-ayuda.json` con ejemplos. Servicio ARI ajustado para `days` dinámico y modo (`generateAriFeed(..., { mode, days })`) y corrección uso de `db` en disponibilidad. **QA formal:** checklist `TASKS/qa-feed-ari-checklist.md`; tests `test-ari-feed-xml-sanity.js` + `test-ari-feed-request.js` en `npm run test:ci`. |
| **Content API / parity de inventario** | **Parcial (2026-04-24):** `websiteSettings.booking` / `websiteData.booking`: estadía **mín./máx.** noches, **anticipación mínima** (días hasta llegada), **ventana de reserva** (meses hacia adelante); fusión empresa + alojamiento; `reservaWebRestriccionesService.js`; `crearReservaPublica`, preview/reconciliación precio, `POST /propiedad/:id/calcular-precio`; widget ficha + calendario público + panel **Configurar sitio web público**. **Hecho (parcial 2026-04-25):** cierre por **día de llegada** vía `diasSemanaLlegadaPermitidos` (empresa + alojamiento, con intersección al fusionar); validación en SSR/checkout + códigos IA/API (`dia_llegada_no_permitido`); test actualizado `test-reserva-web-restricciones.js`. **Hecho (parcial 2026-04-25):** SPA **Gestionar alojamientos** permite editar límites por alojamiento (`websiteData.booking`) para min/max noches, anticipación, ventana en meses y días de llegada; backend sanea/valida en `propiedadesService` con `sanitizePropertyWebsiteBookingIncoming`; test `test-propiedad-booking-sanitize.js`. **Hecho (parcial 2026-04-25):** feed de contenido Google Hotels por tenant `GET /feed-google-hotels-content.xml` (Property List), con token opcional `websiteSettings.integrations.googleHotelsContentToken`; ayuda pública ampliada en `GET /widget-reserva-ayuda.json`; test `test-google-hotels-content-feed-request.js`. **Operación:** checklist por empresa en `TASKS/checklist-onboarding-google-hotel-center.md` (**§9** verificación HTTP/XML automatizada vía `backend/scripts/verify-google-hotels-feed-checklist.js` con `GH_FEED_BASE_URL`). **Pendiente:** onboarding operativo en Google Hotel Center por empresa (validación externa y credenciales). |
| **Multi‑idioma** en SSR | Textos por locale (mínimo EN marketplace) y URLs por idioma si aplica. **Hecho (v4 parcial SEO tenant):** `hreflang` + `x-default` y `og:locale` (`es_CL` / `en_US`) según `idiomaPorDefecto` en home, contacto, ficha, `/reservar`, `/confirmacion`; partial `views/partials/hreflang-default.ejs`; canonical en reserva/confirmacion. **Hecho (v4 copy tenant 2026-04-25):** más strings visibles EN/ES en home (búsqueda, vacíos, grupo, about), contacto, 404, confirmación (depósito + detalle), cabecera/pie, tarjetas del home (`property-card`), `lang`/`og:locale` en ficha cuando `idiomaPorDefecto=en`. **Hecho (v4 parcial marketplace 2026-04-24):** homepage global `/?lang=en` \| `?lang=es` + `Accept-Language` (`en*` → EN); copys ES/EN (`marketplaceUiStrings.js`); `canonical` + `hreflang` es/en/x-default; `og:locale`; selector ES\|EN; formulario preserva `lang`; precios `toLocaleString` según idioma; test `test-marketplace-ui-strings.js`. **Hecho (v4 parcial 2026-04-25):** `GET /api/search.json` unificado (`marketplaceSearchJson.handler.js` desde `index.js` y router marketplace) — `version: 1.1`, `locale`, `language`, `ui.fieldLabels` por `lang`; `sort=valor|valor_desc|rating` aplicado en SQL (`buildMarketplaceOrderBy`) y preservado en links/form; `hreflang` cruzado opcional por dominio en marketplace (`MARKETPLACE_DOMAIN_ES` / `MARKETPLACE_DOMAIN_EN`) con fallback seguro al host actual; smoke XML ARI `ariFeedXmlSanity.js` + `test-ari-feed-xml-sanity.js` + `test-marketplace-sort.js`. |
| **Mapa de calor / restricciones** (eventos locales → min noches) | **Hecho (v4 parcial 2026-04-25):** configuración `websiteSettings.booking.eventosDemandaMapaCalor` saneada en backend (`bookingSettingsSanitize` + `heatmapRestriccionesService`), **edición UI en panel unificado** (`webPublica.general.unified.heatmapEventosRows.js` + `markup/handlers`), calendario público `GET /propiedad/:id/calendario-ocupacion` expone `heatmap[]` (fecha, nivel 1–5, `minNochesLlegada`, motivos) y aplica overlay visual/tooltip en `public-booking-calendar.js`; reglas de estadía mínima por llegada aplicadas en backend (`reservaWebRestriccionesService` + `POST /propiedad/:id/calcular-precio`) usando `max(minNoches base, minNoches mapa)`; `booking.js` usa mínimo efectivo por fecha de llegada. Tests: `test-heatmap-restricciones.js`, `test-reserva-web-restricciones.js`. **Pendiente:** QA E2E de UX/mensajería en propiedades reales y posibles refinamientos de copy por temporada (checklist: `TASKS/qa-heatmap-restricciones-e2e.md`). |
| **House manual digital** (PDF o página) | Adjunto post‑confirmación; complementa normas en web. **Hecho (2026-04-24):** SSR `/confirmacion` enlaza `manualHuespedUrl` / `manualHuespedPdfUrl` / `checkinOnlineUrl` desde `websiteSettings.booking` (misma fuente que correos). |
| **Check‑in online** (datos huésped, documento, hora estimada) | Reduce fricción y fraude; común en Booking/Airbnb. |
| **Reviews outbound** (solicitud post‑estancia) | **Hecho (v1 parcial 2026-04-24):** `[LINK_RESEÑA]` en confirmación + recordatorio pre-llegada vía `resolverLinkResenaOutbound` (además del job post-estancia existente). |
| **Comparador / parity con OTA** (opcional) | “Reserva directa X% más barato” si hay feed de precio externo (complejo). |

### §4.1 Pausa — avance cerrado y pendiente al retomar

**Cerrado en esta iteración (no rehacer al volver):**

- UI en **Configurar sitio web público** (vista unificada): bloque «Legal y checkout público» (cancelación resumida, desglose IVA, notas, JSON de líneas extra con ayuda inline); visibilidad del campo «horas» según modo (`webPublica.general.unified.markup.js`, `webPublica.general.unified.js`, `webPublica.general.unified.handlers.js`).
- **Persistencia:** `PUT /api/website/home-settings` acepta `legal` en cuerpo plano y `websiteSettings.legal` en formato combinado; persiste en `configuracion.websiteSettings.legal` (`backend/routes/websiteConfigRoutes.js`).
- **`lineasExtra`:** módulo compartido por reglas — `backend/services/lineasExtraValidation.js` + `frontend/src/shared/lineasExtraValidation.js` (mantener ambos alineados). Validación por fila antes de guardar en el SPA; en API **400** con `details[]` si falla; al guardar OK se persisten filas **sanitizadas** (mismas reglas que `checkoutDesgloseService` vía `sanitizeLineaExtraDef` importado desde el módulo de validación).
- **Errores en guardado:** el `catch` del botón Guardar muestra `responseJSON.details` cuando el backend rechaza líneas extra.

**Pendiente al retomar (orden sugerido):**

1. ~~**Cancelación en panel vs SSR:**~~ **Hecho (2026-04-24):** con `gratis_hasta_horas`, guardado rechaza horas fuera del rango 1–8760 en SPA (`collectLegalFromForm`) y en **`PUT /api/website/home-settings`** (`validateLegalPoliticaCancelacion` en `checkoutDesgloseService.js`); texto de ayuda bajo el campo; `npm run test:ci` incluye `node backend/scripts/test-legal-politica-cancelacion.js`.
2. **§4 producto (ya listado en tabla):** ~~Cancelación por tarifa; tasas por persona/noche; desglose en presupuesto avanzado (texto + JSON); coherencia de líneas extra vs total de referencia al guardar.~~ → **§4.2**. **Pendiente (opcional / avanzado):** reglas de negocio más finas del anfitrión (p. ej. comparar desglose con tarifador real por reserva) si el producto las define.
3. ~~**UX líneas extra (mejora):**~~ **Hecho (2026-04-24):** editor por filas (`webPublica.general.unified.lineasExtraRows.js`) + textarea oculto sincronizado; misma validación `validateLineasExtraArray` al guardar.
4. ~~**Calidad (mínimo automatizado):**~~ **Hecho (2026-04-24):** `node backend/scripts/test-lineas-extra-validation.js` en `npm run test:ci`. Checklist manual de E2E panel + `PUT home-settings` sigue opcional si quieres formalizarlo.
5. **Otros §4 no tocados:** ~~menores/camas (precio)~~ **parcial (2026-04-24)**; siguen ARI, house manual, check-in online con datos, etc. (ver filas inferiores de la tabla §4).

### §4.2 Desglose / OTA-lite — entregas incrementales (tras §4.1)

**Hecho (2026-04-24):** tipo de línea extra **`por_persona_noche`** (`montoPorPersonaNocheCLP`): validación compartida, `buildDesglosePrecioCheckout(..., { noches, huespedes })`, editor de filas en configuración web unificada, widget ficha (`booking.js` recalcula al cambiar huéspedes).

**Hecho (2026-04-24 — cancelación por tarifa):** `fetchTarifasYCanal` incluye `id` de tarifa; `politicaCancelacionTarifaService.js` (lista de tarifas por noche + merge si una sola tarifa con override); **/reservar**, ficha con fechas en query, **/confirmacion** (desde `metadata.politicaCancelacionCheckout`); **`crearReservaPublica`** persiste snapshot; **matriz** `metadata.politicaCancelacion`; test `test-politica-cancelacion-tarifa.js`.

**Hecho (2026-04-24 — presupuesto):** `generarTextoPresupuesto` (`mensajeService.js`): `buildDesglosePrecioCheckout` con `legal`, noches, huéspedes desde string `personas`; placeholders `[FECHAS_ESTADIA]`, `[GRUPO_HUESPEDES]`, `[DETALLE_PROPIEDADES_PRESUPUESTO]`, `[RESUMEN_VALORES_PRESUPUESTO]`, `[DESGLOSE_PRECIO_PRESUPUESTO]`; plantilla por defecto actualizada. **`presupuestosService.generarPresupuesto`:** persiste `desglosePrecioCheckout` en `datos` cuando la empresa se resuelve. Test `test-presupuesto-desglose.js` en `npm run test:ci`.

**Hecho (2026-04-24 — coherencia desglose):** `validateDesglosePrecioCheckoutCoherencia` en `checkoutDesgloseService.js`; espejo en `frontend/src/shared/desglosePrecioCoherencia.js` + `collectLegalFromForm`; `PUT home-settings` devuelve 400 con `details` si falla; `test-desglose-coherencia.js` en `npm run test:ci`.

**Hecho (2026-04-24 — reconciliación precio web, una propiedad):** `verificarReconciliacionPrecioReservaPublica` en `publicWebsiteService.js`: fechas `YYYY-MM-DD` + `T00:00:00Z` como `POST /propiedad/:id/calcular-precio`; mismo `calculatePrice` (promo tarifa, canal por defecto, USD); `precioFinal` vs total esperado con tolerancia **1 CLP**; coherencia `noches` vs `pricing.nights`; si falla → **409** en `POST /crear-reserva-publica` con `details`; si OK → `metadata.precioCheckoutVerificado` (incl. `subtotalListaCLP`, `recargoMenoresCamasCLP` cuando aplica). **Actualización (2026-04-24):** grupo multi-`propiedadId` reconcilia **suma** de alojamientos + cupón + recargos menores/camas. `checkout.js` concatena `details` al mensaje. Test `test-reconciliacion-precio-reserva-web.js` (tolerancia + fórmula recargo; sin cargar servicio completo).

**Hecho (2026-04-24 — grupo + cupón web v1):** reconciliación **sumada** por cada `propiedadId` (coma); `INSERT` con `propiedad_id` = primer UUID y `metadata.reservaWebGrupo` + nombres concatenados en `alojamiento_nombre`. Cupón opcional (`codigoCupon`): **`porcentaje`** o **`monto_fijo`**; precio enviado vs subtotal (tarifa + recargos) tras cupón; `valores.codigoCupon` / `descuentoCupon`; `marcarCuponComoUtilizado` en misma transacción PG. Campos en `reservar.ejs` + preview.

**Orden de producto acordado (2026):** (1) integridad web: coherencia + reconciliación (incl. grupo sumado y cupón % v1) **hecho**. (2) **Hecho (2026-04-24):** cupón **`monto_fijo`** en reconciliación + preview checkout (`aplicarCuponWebCheckout`); **`POST /preview-precio-reserva-checkout`** + `checkout.js` (sesión previa); transacción **INSERT reserva + `marcarCuponComoUtilizado`** misma conexión PG (`marcarCuponComoUtilizado` acepta `queryClient` opcional). (3) **Hecho (2026-04-24):** política cancelación **multi-tarifa** (restrictiva y mayoría de noches §4.3 B). **Siguiente sugerido:** §4.3 C cerrado en identidad multi-huésped (2026-04-24); otras reglas finas si se formalizan.

**Hecho (2026-04-24 — política cancelación multi-tarifa):** sin **tarifa dominante** (>50 % noches con override), `mergeLegalConPoliticaTarifaUnica` aplica la regla **más restrictiva para el huésped**: `texto_solo` prevalece; si no, `gratis_hasta_horas` con el **mínimo** de horas válidas entre tarifas; si solo `gratis_ilimitada`, ese modo. Misma función en `/reservar`, ficha con fechas, `crearReservaPublica` y snapshot metadata. Test `test-politica-cancelacion-tarifa.js`.

**Hecho (2026-04-24 — fase B cancelación):** mayoría **estricta** de noches (`> 50 %`) con override → política de esa tarifa; empate o minoría → fusión restrictiva como antes. Textos largos distintos al principal → `listarBloquesPoliticaCancelacionLargoDistintosAlPrincipal` + partial SSR; snapshot `politicaCancelacionCheckout.bloquesLargoPorTarifa` en `crearReservaPublica`. Matriz: campo **Nombre en web público** → `metadata.nombre` + hint en copy.

**Pendiente (mismo bloque producto §4 / lista §4.1 ítem 2):** otras reglas finas del anfitrión si se formalizan además de mayoría + bloques.

**Hecho (2026-04-24 — §4.3 C check-in multi-huésped):** flag `checkinIdentidadCoHuespedesActivo` en `websiteSettings.booking`; SSR `/reservar` + `checkout.js`; `metadata.reservaWebCheckout.checkInIdentidadAcompanantes`; validación principal obligatorio si hay acompañantes; retención PII / borrado incluye array; correos `[LISTA_IDENTIDAD_ACOMPANANTES]` / legado; confirmación SSR; panel **gestionar reservas** (detalle reserva) lista read-only de acompañantes. **Hecho (2026-04-24 — house manual en web pública):** SSR `/confirmacion` muestra enlaces a `manualHuespedUrl`, `manualHuespedPdfUrl` y `checkinOnlineUrl` (mismas URLs que plantillas `[LINK_MANUAL_*]` / `[LINK_CHECKIN_ONLINE]`); página dedicada `/guia-huesped`; validación `normalizeBookingUrlForSsr` en `bookingSettingsSanitize.js`.

### §4.3 Roadmap OTA-lite — alcance realista (no todo es una iteración)

«Terminar §4» en sentido **producto OTA completo** incluye integraciones de pago, parity con agregadores, mapas de demanda y formularios de huésped con PII: son **varias fases**. Este documento prioriza entregas incrementales.

| Fase | Contenido | Estado |
|------|-----------|--------|
| **A — Inventario / fechas web** | Min/max noches, anticipación mínima, ventana en meses; validación única servidor + cliente; panel unificado. | **Hecho (2026-04-24):** `reservaWebRestriccionesService.js`, `booking.js`, `public-booking-calendar.js`, `webPublica.general.unified.*`, test `test-reserva-web-restricciones.js`. |
| **B — Cancelación / copy** | Texto legal largo por tarifa; reglas finas multi-tarifa (ponderación). | **Hecho (2026-04-24):** mayoría noches + bloques secundarios SSR; `metadata.nombre` en **matriz**; snapshot `bloquesLargoPorTarifa` + `tarifaIds`; aviso ids vs nombres (`SHARED_CONTEXT` / audit §6b). |
| **C — Huésped / post-reserva** | Check-in online con datos (doc., hora), house manual en correo, reseñas outbound enlazadas. | **Hecho (2026-04-24):** identidad web + consentimiento + borrado/edición panel + **retención opcional por empresa** (`checkinIdentidadRetencionAutomaticaActivo`, `checkinIdentidadRetencionDiasTrasCheckout` en `websiteSettings.booking`; wizard web pública; job `npm run job:retencion-checkin-identidad-pii` + auditoría `eliminadoMotivo` / `diasPoliticaRetencion`). **Hecho (2026-04-24):** `[LINK_RESEÑA]` en **confirmación** y **recordatorio pre-llegada**; URL externa o token `/r/…` alineado a post-estancia. **Hecho (2026-04-24):** check-in **multi-huésped** opcional (`checkinIdentidadCoHuespedesActivo`, `checkInIdentidadAcompanantes`, correo/confirmación/panel). **Hecho (2026-04-24):** hora estimada de llegada y bloque llegada ligera del checkout alineados al mismo PII que identidad (detección, panel, job). **Hecho (2026-04-24):** house manual / check-in online en **`/confirmacion`** + **página dedicada `/guia-huesped`** (enlaces web + PDF + URL check-in, i18n por `idiomaPorDefecto`). |
| **D — Parity “OTA dura”** | Operación manual de pagos (sin pasarela), Content API Google, mapa calor, comparador externo. | **Parcial (2026-04-25):** comparador externo base en SSR tenant `GET /propiedad/:id/comparador-ota.json?fechaLlegada=YYYY-MM-DD&fechaSalida=YYYY-MM-DD[&canalId=...]` (canal directo = por defecto vs canal comparado; totales CLP, ahorro CLP/% y flag `comparableComplete` si faltan noches en canal comparado); cálculo referencial sin persistencia financiera (`comparadorOtaService.js`), test `test-comparador-ota-service.js`. **Hecho (parcial 2026-04-25):** ficha SSR (`booking-widget`) muestra bloque “Comparador reserva directa” al cambiar fechas, solo cuando `comparableComplete=true`, y permite elegir canal comparado (selector en UI; recálculo por `canalId`) (`booking.js` + `partials/booking-widget.ejs` + `website.property.js`). **Hecho (parcial 2026-04-25):** copy/legal final en UI del comparador (`legalCopy`) + monitoreo en logs backend por consulta (`[comparador-ota]` JSON estructurado con tenant/propiedad/canales/ahorro/completitud). **Hecho (parcial 2026-04-25):** mapa de calor con ciclo completo (config panel + API `heatmap[]` + overlay/tooltip + validación automática de noches mínimas por fecha de llegada en SSR/checkout). **Pendiente:** QA E2E y ajustes de UX/copy según comportamiento real por tenant. |

**Cierre / operación (fuera de §4 OTA-lite de catálogo):** métricas iCal en PG (**§1.4**), ~~wizard DNS legacy (**§3.1**)~~ **cerrado 2026-04-24**, widget tarifas con fechas (**§2.3** hecho), marketplace i18n global (**§4** fila multi-idioma).

---

## 5. Orden sugerido de implementación (opinión)

**Revisión (2026-04-22):** motor por disparadores, bandeja **1.1**, digest **1.4c** + push opcional, checkout con política cancelación SSR, ofertas tarifa **§2**. **Nuevos eventos de correo (§1.2)** van **al final** (no competir con el orden siguiente).

1. **Superficie pública y OTA-lite (§4)** — impuestos/tasas y desglose en presupuesto/checkout; políticas de cancelación estructuradas (tipos/plazos); menores/camas extra si prioridad; más SEO i18n donde falte (p. ej. marketplace).  
2. **Preferencias y copy del motor (§1.6)** — asuntos y textos fijos del motor según locale; más categorías de `correosAutomaticosCategorias` si el producto las define.  
3. **Dominio y DNS (§3)** — ~~wizard legacy~~ retirado (§3.1); seguir mejoras `dns-sync-error` / API si el producto lo pide.  
4. **Tarifas y marketplace (§2)** — embed CTA + fechas embebidas (**2.3e** hecho); mantener alineación promo checkout/calendario.  
5. **Alertas operación (§1.4)** — métricas iCal en PG si se persisten; más tipos de alerta sin ampliar aún la matriz de *correo* salvo decisión explícita.  
6. **Último — eventos transaccionales nuevos (§1.2)** — workshop producto: lista de eventos, disparadores, audiencia (huésped/equipo), registro en `comunicaciones`, semillas `scripts/seed-plantillas-correos-transaccionales.js`, filas en `transactionalEmailEventMatrix.js` y hooks en código.

## 5.x Checklist operativo — cierre pendientes actuales (2026-04)

### A) Mapa de calor / restricciones (QA E2E tenant real)

- [ ] Ejecutar QA E2E en al menos 2 propiedades reales por tenant:
  - [ ] Configuración panel (`eventosDemandaMapaCalor`) persiste y sanea correctamente.
  - [ ] `GET /propiedad/:id/calendario-ocupacion` devuelve `heatmap[]` esperado.
  - [ ] Calendario SSR muestra overlay/tooltip coherente con reglas.
  - [ ] Checkout respeta `minNoches` efectivo por fecha de llegada.
- [ ] Revisar copy/UX de mensajes (bloqueo por llegada, mínimos por evento) y definir ajustes.
- [ ] Documentar evidencia (capturas/logs mínimos) y cerrar pendiente §4 / §4.3 D.
- **Estado objetivo:** LISTO + sin bloqueos de negocio en propiedades reales.

### B) Google Hotel Center por empresa (onboarding operativo)

- [ ] Ejecutar `TASKS/checklist-onboarding-google-hotel-center.md` por tenant.
- [ ] Validar feed ARI y feed content expuestos por dominio real de cada empresa.
- [ ] Confirmar token/credenciales activas por tenant (`websiteSettings.integrations.*`).
- [ ] Completar validación externa en Google Hotel Center (HTTP/XML/propiedades visibles).
- [ ] Registrar por empresa: `operativo` / `bloqueado` + causa concreta + siguiente acción.
- **Estado objetivo:** tenant con integración realmente operativa, no solo "endpoint existe".

### C) §4.3 D parity OTA/comparador (cierre MVP)

- [ ] Definir DoD explícito del comparador:
  - [ ] ¿MVP queda en "referencial + legalCopy + selectable canal"?
  - [ ] ¿Se exige algo más para cierre (copy, tolerancias, reglas de visualización)?
- [ ] Aplicar ajustes mínimos acordados (si corresponde) y validar en ficha SSR.
- [ ] Dejar criterio final en backlog para evitar estado "parcial" indefinido.
- **Estado objetivo:** LISTO con alcance acotado y verificable.

### D) Pendientes diferidos (no bloqueantes release)

- [ ] Mantener **al final del roadmap** los nuevos eventos/disparadores de correo (§1.2 / §5.6).
- [ ] Tratar reglas finas avanzadas (cancelación/desglose) solo con definición formal de producto.
- [ ] Evaluar expansión de check-in online profundo en ciclo v1.1 (no mezclar con cierre operativo).

### E) Criterio de salida de este bloque

- [ ] A + B cerrados.
- [ ] C con DoD explícito y estado final (`LISTO` o `PAUSA` justificada).
- [ ] Backlog actualizado con fecha, evidencia mínima y "siguiente foco" para v1.1.

**Entrega parcial (2026-04-22):** motor de correos por disparadores (`transactionalEmailService.js`), hooks en gestión/reservas, jobs (`backend/jobs/scheduledTransactionalEmails.js`), formulario `POST /contacto` SSR, promos en tarjetas vía `websiteSettings.marketing.promocionesDestacadas`, texto/HTML de política de cancelación en ficha (`websiteSettings.legal.politicaCancelacionTexto` / `politicaCancelacionHtml`), orden marketplace `sort=valor` en `/api/search.json`.

**Extensión misma fecha:** bandeja `GET /api/comunicaciones` + vista `/comunicaciones`; columna `tarifas.metadata` (migración `backend/db/migrations/tarifas-metadata-promo.sql`), matriz con oferta web, SSR home/ficha con `aplicarPromocionesDisplayCompleto` (`promocionesDisplayService.js` + `website.shared.js` cargando `metadata`).

**Sembrado inicial de plantillas transaccionales (idempotente, por empresa):** desde la raíz del repo, con PostgreSQL activo y `DATABASE_URL` en `backend/.env`:

```bash
node scripts/seed-plantillas-correos-transaccionales.js
```

No duplica disparadores: solo inserta si la empresa aún no tiene una plantilla activa con ese `email_config.disparadores`.

---

## 6. Referencias rápidas en el repo

| Área | Archivos / rutas |
|------|------------------|
| Email | `backend/services/emailService.js` |
| Reserva / propuesta email | `backend/services/gestionPropuestas.email.js` |
| Textos con plantillas | `backend/services/mensajeService.js`, `backend/routes/mensajes.js` |
| Registro envíos + bandeja tenant | `comunicacionesService.js` (`listarHilosComunicacionesEmpresa`), `comunicacionesRetryService.js`, `routes/comunicaciones.js` (`GET /comunicaciones/hilos`, reintentos), `frontend/src/views/comunicaciones.js` (v1.1f: error de carga, reintentar lista, toasts) |
| Ofertas tarifa + SSR display | `backend/db/migrations/tarifas-metadata-promo.sql`, `backend/services/tarifasService.js`, `backend/routes/website.shared.js`, `backend/services/promocionesDisplayService.js`, `frontend/src/views/components/gestionarTarifas/matriz.js` |
| Marketplace precio desde + descubrimiento ficha | `backend/services/marketplaceService.js`, `backend/views/marketplace/index.ejs`, `backend/routes/marketplace.js`, `backend/services/publicWebsiteService.js` (`obtenerMasAlojamientosParaFichaSSR`), `backend/routes/website.property.page.js` |
| Dominio Render | `backend/services/renderDomainService.js`, flujo `home-settings` en `backend/api/ssr/config.routes.js` |
| Google Hotels / feed ARI | `backend/services/googleHotelsService.js` (`generateAriFeed`, `generatePropertyListFeed`), `backend/routes/website.seo.js` (`/feed-ari.xml`, `/feed-google-hotels-content.xml`, `/widget-reserva-ayuda.json`), `backend/scripts/verify-google-hotels-feed-checklist.js`, `TASKS/checklist-onboarding-google-hotel-center.md`, `backend/routes/integrations.js`, `frontend/src/shared/ariFeedUrl.js`, `empresa.js`, `sincronizarCalendarios.js`, `webPublica.general.unified.{js,markup.js}` |
| Motor correo + hooks | `backend/services/transactionalEmailService.js` (`resolverLinkResenaOutbound`, `construirVariablesDesdeReserva`), `backend/services/transactionalEmailHooks.js`, `backend/services/transactionalEmailEventMatrix.js` (`GET /plantillas/matriz-eventos-correo`) |
| Jobs correo / digest | `backend/jobs/scheduledTransactionalEmails.js` (arranque en `backend/index.js`) |
| Semilla plantillas correo | `scripts/seed-plantillas-correos-transaccionales.js` |
| SEO tenant (hreflang / og:locale / copy EN) | `backend/routes/website.context.js` (`res.locals.htmlLang`), `backend/views/partials/hreflang-default.ejs`, `partials/header.ejs`, `partials/footer.ejs`, `partials/property-card.ejs`, `website.booking.js`, `home.ejs`, `contacto.ejs`, `propiedad.ejs`, `reservar.ejs`, `confirmacion.ejs`, `404.ejs` |
| Checkout desglose / cancelación resumen (§4) | `checkoutDesgloseService.js`, `lineasExtraValidation.js`, `politicaCancelacionTarifaService.js`, `reservaWebRestriccionesService.js`, `publicWebsiteService.js` (`verificarReconciliacionPrecioReservaPublica`, `crearReservaPublica`), `website.booking.js` (`POST /crear-reserva-publica` 409), `website.property.js` (`calcular-precio`), `website.property.page.js`, `website.shared.js`, `partials/checkout-desglose-bloque.ejs`, `partials/aviso-politica-cancelacion.ejs`, `partials/politica-cancelacion-bloques-secundarios.ejs`, `booking-widget.ejs`, `public/js/booking.js`, `public/js/public-booking-calendar.js`, `public/js/checkout.js`, `gestionarTarifas/matriz.js`; §4.3 C: `reservaWebCheckinIdentidadService.js`, `reservaWebCheckinIdentidadRetencionService.js`, `transactionalEmailService.construirVariablesDesdeReserva`, `gestionPropuestas.email.js`, `reservar.ejs` / `confirmacion.ejs`, `gestionarReservas.js` + `reservas.modals.view.js`, `plantillasEtiquetasCatalog.js` |
| Panel legal/checkout (SPA) + `home-settings` | `frontend/src/views/components/configurarWebPublica/webPublica.general.unified.{js,markup.js,handlers.js}`, `webPublica.general.unified.lineasExtraRows.js` (líneas extra por filas), `frontend/src/shared/lineasExtraValidation.js`, `frontend/src/shared/desglosePrecioCoherencia.js`, `backend/api/ssr/config.routes.js` (`PUT /home-settings`, merge `legal`) |
| Términos y condiciones (OTA, configurable) | `websiteSettings.terminosCondiciones` (ES + `seccionesEn` EN); plantilla `terminosCondicionesDefaults.js`; SPA `terminosCondiciones.js`; SSR `/terminos-y-condiciones`; pie + **cabecera** + checkout: checkbox obligatorio si `publicado` (`reservar.ejs`, `checkout.js`, `crearReservaPublica` → `metadata.aceptacionTerminos`); API plantilla + `home-settings`; `empresaService` merge; sitemap condicional `website.seo.js` |

---

*Última actualización: 2026-04-25 — **Google Hotels / SSR / bandeja:** checklist §9 + script `verify-google-hotels-feed-checklist.js`; `htmlLang` global + i18n ampliada (home, contacto, 404, confirmación, header/footer, property-card, ficha `og:locale`); bandeja comunicaciones UX v1.1f; comentarios/JSDoc multi-tenant calendario y feed contenido. **Release 1.0.0 (puerta técnica):** `npm run test:ci` ampliado en raíz (`package.json`) con scripts alineados a `TASKS/plan-release-1.0.0.md` §2.1; CI GitHub `.github/workflows/ci-smoke.yml` ejecuta la misma cadena. Smoke manual §2.3 en staging + tag `v1.0.0` + push quedan según plan y `TASKS/leer-primero.md` §5.1. Mapa de calor §4: pendiente QA E2E tenant real. Post-tag: §4.3 D + §5 según prioridad.*
