/**
 * Re-export del agregador global (implementación en `../googleHotelsGlobalService.js`).
 * Mantener este path por compatibilidad con imports existentes (TASKS/tema/SM-venta-ia/venta-ia.md §6).
 */
module.exports = require('../googleHotelsGlobalService');
