<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Completar Reserva | <%= empresa.nombre %></title>
    <meta name="description" content="Completa los datos para tu reserva en <%= propiedad.nombre %>." />
    <link rel="stylesheet" href="/public/css/website.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
</head>
<body class="bg-gray-50">

    <%- include('partials/header') %>

    <main class="container py-12">
        <h1 class="text-3xl font-bold text-gray-900 mb-8">Completa tu Reserva</h1>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-12">
            
            <div class="lg:col-span-2 bg-white p-8 rounded-lg shadow-lg">
                <h2 class="text-2xl font-semibold text-gray-800 mb-6">Ingresa tus Datos</h2>
                
                <form id="checkout-form" class="space-y-4">
                    <input type="hidden" name="propiedadId" value="<%= propiedad.id %>">
                    <input type="hidden" name="fechaLlegada" value="<%= query.fechaLlegada %>">
                    <input type="hidden" name="fechaSalida" value="<%= query.fechaSalida %>">
                    <input type="hidden" name="noches" value="<%= query.noches %>">
                    <input type="hidden" name="precioFinal" value="<%= query.precioFinal %>">
                    <input type="hidden" name="personas" value="<%= query.personas %>">

                    <div>
                        <label for="nombre" class="block text-sm font-medium text-gray-700">Nombre Completo</label>
                        <input type="text" id="nombre" name="nombre" class="form-input mt-1" required>
                    </div>
                    
                    <div>
                        <label for="email" class="block text-sm font-medium text-gray-700">Email</label>
                        <input type="email" id="email" name="email" class="form-input mt-1" required>
                    </div>
                    
                    <div>
                        <label for="telefono" class="block text-sm font-medium text-gray-700">Teléfono (Ej: +569...)</label>
                        <input type="tel" id="telefono" name="telefono" class="form-input mt-1" required>
                    </div>
                    
                    <div>
                        <label for="comentarios" class="block text-sm font-medium text-gray-700">Comentarios Adicionales (Opcional)</label>
                        <textarea id="comentarios" name="comentarios" rows="3" class="form-input mt-1"></textarea>
                    </div>

                    <div class="pt-6 border-t">
                        <button type="submit" class="btn w-full text-lg">
                            Confirmar Reserva
                        </button>
                        <div id="status-message" class="text-sm text-center mt-4"></div>
                    </div>
                </form>
            </div>

            <div class="lg:col-span-1">
                <div class="bg-white p-6 rounded-lg shadow-lg sticky top-8">
                    <h3 class="text-xl font-bold text-gray-900 mb-4">Resumen de tu Estadía</h3>
                    
                    <% 
                        // Obtener imagen principal (usando la misma lógica de 'propiedad.ejs')
                        const imagenes = propiedad.websiteData?.images || {};
                        const todasLasImagenes = Object.values(imagenes).flat();
                        const imagenPrincipal = imagenes['portadaRecinto']?.[0] || imagenes['exteriorAlojamiento']?.[0] || todasLasImagenes[0];
                        const imagenUrl = imagenPrincipal?.storagePath || 'https://via.placeholder.com/400x300.png?text=Imagen+no+disponible';
                        const altText = imagenPrincipal?.altText || `Imagen de ${propiedad.nombre}`;
                    %>
                    
                    <img src="<%= imagenUrl %>" alt="<%= altText %>" class="w-full h-40 object-cover rounded-lg mb-4">
                    
                    <h4 class="text-lg font-semibold"><%= propiedad.nombre %></h4>
                    
                    <div class="mt-4 space-y-2 border-t pt-4">
                        <div class="flex justify-between">
                            <span class="text-gray-600">Llegada:</span>
                            <span class="font-medium"><%= new Date(query.fechaLlegada + 'T00:00:00-03:00').toLocaleDateString('es-CL', { day: '2-digit', month: 'short', year: 'numeric' }) %></span>
                        </div>
                        <div class="flex justify-between">
                            <span class="text-gray-600">Salida:</span>
                            <span class="font-medium"><%= new Date(query.fechaSalida + 'T00:00:00-03:00').toLocaleDateString('es-CL', { day: '2-digit', month: 'short', year: 'numeric' }) %></span>
                        </div>
                         <div class="flex justify-between">
                            <span class="text-gray-600">Noches:</span>
                            <span class="font-medium"><%= query.noches %></span>
                        </div>
                         <div class="flex justify-between">
                            <span class="text-gray-600">Huéspedes:</span>
                            <span class="font-medium"><%= query.personas %></span>
                        </div>
                        
                        <div class="flex justify-between text-2xl font-bold text-gray-900 border-t pt-4 mt-4">
                            <span>Total</span>
                            <span>$<%= parseInt(query.precioFinal).toLocaleString('es-CL') %></span>
                        </div>
                        <p class="text-xs text-gray-500 text-center">(CLP)</p>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <%- include('partials/footer') %>
    
    <script src="/public/js/checkout.js" defer></script>
</body>
</html>